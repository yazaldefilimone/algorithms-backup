fn main() {
    println!("Hello, world!");
}

fn quicksort(elements: &vec!<i64>) -> &'static [i64] {
    if elements.len() < 2 {
        return elements;
    } else {
  
       
    }
}

fn isBigger(e: i64, quite:i64) -> bool{
    return  e > quite
}

fn isSmaller(e: i64, quite:i64)-> bool{
    return  e < quite
}